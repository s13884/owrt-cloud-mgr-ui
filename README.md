# owrt-cloud-mgr-ui
Frontend For Cloud Solution 


## Containerized React development environment 
Clone directory
git clone $URL

cd /path/to/dev-dir

## Run following command to setup the environment
docker-compose up --build

docker-compose run --rm openwrt-ui-dev_frontend sh -c "npm install"

## Run Container and start development
docker-compose up